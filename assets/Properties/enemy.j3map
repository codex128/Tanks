tank: {
    color: "black";
    class: "defensive";
    speed: 8f;
    rps: 2.5f;
    maxBullets: 4;
    maxBounces: 1;
    maxMines: 2;
    bulletSpeed: 27f;
    color1: ColorRGBA(0.3, 0.0, 0.6, 1.0);
}
bulletAlert: {
    alert: 10f;
}
defensivePoints: {
    pointAngle: .25f;
}
directShot: {
    exposureThreshold: .5f;
}
randomPoints: {
    maxPointDistance: 10f;
}
sniper: {
    barrelSpeed: .05f;
}
wander: {
    previousTurnBias: 2.0f;
    turnSpeed: 0.05f;
    wander: 1f;
    directional: 1f;
}
